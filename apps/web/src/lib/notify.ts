// src/lib/notify.ts
export const notifySuccess = (message: string) => {
  alert(message); // replace later with toast system
};

export const notifyError = (message: string) => {
  alert(message);
};
